version: '3'
services: 
  weatherapplication:
    build: 
     context: .
     dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/go/src/app
 # tests:
 #   build: 
 #    context: .
 #    dockerfile: Dockerfile.dev
 #   volumes:
 #     - .:/go/src/app